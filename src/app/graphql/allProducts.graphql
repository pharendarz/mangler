query allProductsQuery($date: Date!) {
    productTypes(businessDate: $date) {
        line
        products(first: 99) {
            edges {
                node {
                    productId
                }
            }
        }
    }
}
